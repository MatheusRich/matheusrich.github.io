#!/usr/bin/env bash

cd "$(dirname "$0")/.."
if [ -z "$1" ]; then
  echo "Usage: bin/optimize-images <directory>"
  echo "Example: bin/optimize-images assets/img/2025-in-review"
  exit 1
fi

bundle exec bake optimize_images "$1"
